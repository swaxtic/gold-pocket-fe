import { TitleformatPipe } from './titleformat.pipe';

describe('TitleformatPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleformatPipe();
    expect(pipe).toBeTruthy();
  });
});
